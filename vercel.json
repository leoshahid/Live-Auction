{
  "version": 2,
  "builds": [
    {
      "src": "auction-simulcast/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build"
      }
    }
  ],
  "routes": [
    {
      "src": "/static/(.*)",
      "dest": "/auction-simulcast/build/static/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/auction-simulcast/build/index.html"
    }
  ]
}
